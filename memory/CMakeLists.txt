# Only included for modularity. No need to do anything unless you add .cpp files later.
cmake_minimum_required(VERSION 3.8)
project(memory_dummy)

# Nothing to do here, as memory is included in the root CMakeLists.txt